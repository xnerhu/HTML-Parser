using System.Collections.Generic;

namespace HTMLParser {
    public static class DOMBuilder {
        public static List<Node> BuildTree(List<string> tokens) {
            List<Node> tree = new List<Node>();

            return tree;
        }
    }
}
